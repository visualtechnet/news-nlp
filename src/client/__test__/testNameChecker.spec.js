import { checkForName } from '../js/nameChecker'

describe("Testing the other functionality", () => {
    test("Testing the namechecker() function", () => {
        expect(checkForName).toBeDefined();
})});